export default function HomePage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col items-start justify-center gap-4 px-6 py-16">
      <h1 className="text-4xl font-bold tracking-tight text-[hsl(var(--primary))]">
        Showalter Services
      </h1>
      <p className="text-base text-[hsl(var(--muted-foreground))]">
        Phase 0 skeleton — the rest of the site lands in subsequent phases.
      </p>
    </main>
  );
}
